<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Astral Tech Blog</title>
    <link rel="stylesheet" href="/css/jass.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class = "flex-column justify-space-around">
        <header class = "display-flex justify-space-between align-center flex-row p-2 mb-5 background">
            <h1 class = "mx-auto mb-5">
                <a href="/">Astral Tech Blog</a>
            </h1>
            <nav class = "display-flex justify-space-between col-md-12 mx-auto">
                <a href="/">Home</a>
                <a href="/">Dashboard</a>
                {{#if logged_in}}
                <button class = "no-button" id="logout">Logout</button>
                {{else}}
                <a href="/login">Login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{!-- {{#if logged_in}}
            <p class = "ml-5">Welcome {{username}}!</p>
            {{/if}} --}}
            {{{ body }}}
        </main>
    </div>

    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>
</html>